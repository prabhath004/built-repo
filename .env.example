# Optional. When set, /api/analyze uses the OpenAI API for the intake analysis.
# When unset (or on any API error), ImplFlow transparently falls back to the
# built-in deterministic rules engine — so the demo always works.
OPENAI_API_KEY=

# Optional. Defaults to gpt-4o-mini.
OPENAI_MODEL=gpt-4o-mini
